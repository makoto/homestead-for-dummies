{
  "name": "dappsforbeginners",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "check-dependencies && webpack",
    "watch": "npm run build -- --watch --devtool inline-source-map",
    "test": "check-dependencies && mocha --compilers js:babel-register --recursive spec/js",
    "test-debug": "check-dependencies && mocha debug --compilers js:babel-register --recursive spec/js",
    "lint": "check-dependencies && eslint js --ext=jsx,js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^6.7.0",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.5.0",
    "solc": "^0.3.1-1",
    "web3": "^0.15.3",
    "webpack": "^1.12.14"
  },
  "babel": {
    "presets": [
      "es2015",
      "react"
    ]
  }
}
